---
# =============================================================================
# IAS-Ansible Staging Inventory
# =============================================================================
# Usage: make deploy ENV=staging

all:
  vars:
    ansible_user: root
    ansible_python_interpreter: auto_silent

  children:
    auto_update:
      hosts: {}
    # Add staging hosts here
