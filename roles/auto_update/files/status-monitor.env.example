# =============================================================================
# IAC-Ansible: Status Monitor Configuration
# =============================================================================
# This file configures the automatic maintenance window integration.
# Before updates, a maintenance window is opened in your status monitor.
# After updates (or after reboot), the window is closed automatically.
#
# SETUP:
#   1. Copy this file:
#      cp /etc/iac-ansible/status-monitor.env.example /etc/iac-ansible/status-monitor.env
#   2. Edit the values below
#   3. Set permissions:
#      chmod 600 /etc/iac-ansible/status-monitor.env
#      chown root:root /etc/iac-ansible/status-monitor.env
#
# ALTERNATIVE: Deploy via Ansible Vault (see update_settings.yml)
# =============================================================================

# --- Provider ---
# Available: "uptime-kuma"
# Add custom providers in /usr/local/lib/iac-ansible/providers/
STATUS_MONITOR_PROVIDER="uptime-kuma"

# --- Connection ---
# Uptime Kuma base URL (no trailing slash)
STATUS_MONITOR_URL="https://status.example.com"

# Login credentials (Uptime Kuma uses username/password via Socket.IO API)
STATUS_MONITOR_USERNAME=""
STATUS_MONITOR_PASSWORD=""

# --- Monitor Discovery ---
# Monitors are found automatically by matching the server's FQDN hostname
# against monitor names, URLs, and hostnames in Uptime Kuma.
#
# Example: Server FQDN "0046-20.cloud.bauer-group.com" will match monitors
# named "0046-20.cloud.bauer-group.com HTTP", URL "https://0046-20.cloud...",
# or hostname field "0046-20.cloud.bauer-group.com".
#
# Optional: Override the hostname used for discovery (default: FQDN)
# STATUS_MONITOR_HOSTNAME="custom-hostname.example.com"
