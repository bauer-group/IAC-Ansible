---
- name: Converge
  hosts: all
  become: true
  gather_facts: false

  vars:
    secrets_backend: "ansible-vault"
    # No secrets.yml present — tests graceful degradation (empty defaults)

  roles:
    - role: secrets
