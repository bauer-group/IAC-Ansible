---
- name: Converge
  hosts: all
  become: true
  gather_facts: true

  vars:
    # Test image is NOT DinD-capable — deploy configs but don't start daemon.
    docker_enabled: false
    docker_ipv6_nat_enabled: false
    docker_prune_enabled: true

  roles:
    - role: docker
