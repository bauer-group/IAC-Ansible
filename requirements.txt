ansible>=13.0,<14.0
ansible-lint>=26.0,<27.0
yamllint>=1.38,<2.0
hvac>=2.1,<3.0
netaddr>=1.2,<2.0
molecule>=26.0,<27.0
# molecule-plugins is versioned independently from molecule (latest is 25.x,
# no 26.x); it requires molecule with no upper bound, so it works with 26.x.
molecule-plugins[docker]>=25.0,<26.0
pytest-testinfra>=10.0,<11.0
